[Definition]
# 对frps认证失效的用户进行匹配
# 多行匹配：先匹配 login info 行（记录IP），紧接着下一行必须是 token 错误
failregex = ^.*client login info: ip \[<HOST>:[0-9]+\].*\n\s*.*register control error: token in login doesn't match

# 多行模式
maxlines = 2

ignoreregex =

datepattern = ^%%Y-%%m-%%d %%H:%%M:%%S