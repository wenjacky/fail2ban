# 客户端：接收本机 UDPspeeder 的 44101 UDP，并连接服务端公网 44101 raw 端口。
# -a 会自动管理 iptables 规则；密钥、raw 模式和加密方式必须与服务端一致。
OPTS=-c -l127.0.0.1:44101 -r47.245.124.39:44101 -k spdwg_sg2gz_wjg --raw-mode faketcp --cipher-mode aes128cbc -a
