# 客户端：WireGuard 把 UDP 发送到本机 22180；udp2raw 再连接服务端公网 21180。
# -a 会自动管理 iptables 规则；部署前把公网 IP、端口和密钥与服务端配置核对一致。
OPTS=-c -l127.0.0.1:22180 -r47.245.124.39:21180 -k wg_gz2sg_wjg --raw-mode faketcp --cipher-mode aes128cbc -a
