# 服务端：监听公网 24096 raw 端口，把还原的 UDP 转发给本机 tinyfecVPN 的 44096。
# -a 会自动管理 iptables 规则，服务端安全组也必须放行对应 raw-mode 流量。
OPTS=-s -l0.0.0.0:24096 -r127.0.0.1:44096 -k tinyvpn_wjg --raw-mode faketcp --cipher-mode aes128cbc -a
