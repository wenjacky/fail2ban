# 服务端：监听公网 21180 raw 端口，把还原的 UDP 转发给本机 WireGuard 的 22180。
# -a 会自动管理 iptables 规则；密钥、raw 模式和加密方式必须与客户端一致。
OPTS=-s -l0.0.0.0:21180 -r127.0.0.1:22180 -k wg_gz2sg_wjg --raw-mode faketcp --cipher-mode aes128cbc -a
